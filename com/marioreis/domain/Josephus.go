package domain

/*
import "slices"

func Josephus(n int, index int) int {
	arr := make([]int, n)
	count := 1

	for i := 1; i <= n; i++ {
		arr[i-1] = i
	}

	for count < len(arr) {
		actualIndex := index - 1

		if count > 1 {
			actualIndex = len(arr) - count
		}

		//slices.Concat
		//slices.Concat

		arr[actualIndex] = 0
		count++
	}

}
*/
